# QR Code Generator
A simple Python GUI application to generate QR codes from text or URLs using Tkinter and the `qrcode` library.



## Features
> Generate QR codes from any text or URL.
> Choose a save location and file name for the QR code image.
> Select QR code size (version 1-40).
> Automatically creates folders if the save location doesn’t exist.
> Provides error messages for invalid inputs.

## Installation
1. Clone the repository:
   ```bash
   git clone <https://github.com/dharshanamesh04/FiletoQRcode>
   cd QR_Code_Generator
   
2.Install dependencies:
bash:
Copy,Edit
>>"pip install -r requirements.txt"

Usage:
1.Run the application:
bash:
Copy,Edit
>>"python qr_code_generator.py"

2.Fill in the following fields in the GUI:
Text/URL: The content you want to encode.
Save Location: Folder where the QR code image will be saved.
File Name: Name of the QR code image.
Size (1-40): QR code version (higher number = more data capacity).
Click Generate Code to create and save your QR code.

3.Requirements
Python 3.x
Tkinter (usually included with Python)
qrcode library
Pillow library

License
This project is open-source and free to use.











